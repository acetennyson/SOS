import { WebPlugin } from '@capacitor/core';
import { PrivacyScreenConfig, PrivacyScreenPlugin } from './definitions';
export declare class PrivacyScreenWeb extends WebPlugin implements PrivacyScreenPlugin {
    private enabled;
    enable(_config?: PrivacyScreenConfig): Promise<{
        success: boolean;
    }>;
    disable(): Promise<{
        success: boolean;
    }>;
    isEnabled(): Promise<{
        enabled: boolean;
    }>;
}
